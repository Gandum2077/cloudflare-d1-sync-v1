# cloudflare-d1-sync 同步协议设计

## 1. 目标

`cloudflare-d1-sync` 是基于 Cloudflare Workers + D1 的通用数据同步服务。

服务端不感知具体业务结构。所有业务数据统一以 JSON 保存，使客户端未来新增表、字段或数据类型时，无需修改 Worker 或迁移 D1 Schema。

核心设计原则：

* 服务端仅负责数据存储、版本控制和同步。
* `content` 对服务端是不透明的 JSON 数据。
* `sync_version` 用于单条数据的并发控制。
* `seq` 用于设备间的增量同步。
* `changes` 仅保存有限数量的近期变化。
* `data` 始终保存完整的当前状态，包括删除记录。
* 绝对禁止全表扫描和单次全表读取；所有运行时查询、同步与清理必须通过索引定位，并限定读取范围。

### 1.1 读取额度的绝对约束

- `changes` 的去重、比较和合并只能作用于当前页，禁止在整张 changes 上执行分组、窗口函数或为每条记录搜索历史最新事件。
- 增量同步先通过 seq 索引读取当前页，再在内存中合并；随后只按复合主键精确读取本页涉及的 data，不得扫描 data 或解析 content 查找记录。
- 完整下载只能按 `(tablename, id)` 主键索引进行游标分页，禁止 OFFSET、先读取全表再切页、无界排序、全表计数和无界查询。
- `limit` 默认 100、最大 100，必须是 1～100 的整数；增量页按原始事件数量计，完整下载页按记录数量计。可以主动使用更小的 limit。
- 分页有无后续数据可以另做一次索引定位并读取最多一条键；不能先读完整结果计算总数。
- 最新/最早 seq 只能从 seq 索引端点读取一行。该端点定位及游标范围筛选不属于跨页记录合并。
- 查询是否合规以实际执行计划与读取行数为准，不能仅凭 SQL 有 LIMIT 判断。禁止无界表扫描或无界索引遍历；首个下载页允许从主键索引起点读取最多 limit 条。
- 完整下载累计会读取全部记录，这是用户主动恢复数据所需的读取量；不能将分页解释为免除累计读取额度。

---

## 2. 数据表

### 2.1 data

保存所有业务数据的当前状态。

```sql
CREATE TABLE data (
    tablename TEXT NOT NULL,
    id TEXT NOT NULL,

    content TEXT,
    sync_version INTEGER NOT NULL,
    deleted INTEGER NOT NULL DEFAULT 0,

    server_updated_at INTEGER NOT NULL,

    created_by_device_id TEXT,
    updated_by_device_id TEXT,

    PRIMARY KEY (tablename, id)
);
```

字段说明：

| 字段                     | 说明          |
| ---------------------- | ----------- |
| `tablename`            | 客户端逻辑表名     |
| `id`                   | 数据 ID       |
| `content`              | JSON 格式业务数据 |
| `sync_version`         | 当前数据版本      |
| `deleted`              | 是否已删除       |
| `server_updated_at`    | 服务端最后更新时间   |
| `created_by_device_id` | 创建数据的设备     |
| `updated_by_device_id` | 最后修改数据的设备   |

`tablename + id` 唯一标识一条数据。

服务端不得依赖或查询 `content` 内部结构。

`tablename`、`id` 均为字符串，不限制格式，不自动 trim、规范化或转换大小写；空字符串也属于合法字符串。唯一性和排序使用 SQLite 默认 BINARY 排序，客户端必须原样保存服务端分页键，不自行重排。两者均作为绑定值，不能拼成 SQL 标识符；内存去重使用二元组，不能用分隔符拼接造成键碰撞。

create/update 必须提供完整 `content`，接受任意合法 JSON 值，包括 JSON null。协议不设 content 大小上限，也不增加字符串长度上限；实际请求和存储仍受 Cloudflare 平台硬限制，不得静默截断。JSON null 序列化为文本 `null`，与 tombstone 的 SQL NULL 区分。

---

### 2.2 changes

记录近期发生的变化事件。查询、分页和清理均使用 `seq` 主键，不为 `(tablename, id)` 或 `device_id` 建立额外索引，以避免每次追加事件多消耗两次 D1 写入。

```sql
CREATE TABLE changes (
    seq INTEGER PRIMARY KEY AUTOINCREMENT,

    tablename TEXT NOT NULL,
    id TEXT NOT NULL,

    operation TEXT NOT NULL,
    device_id TEXT NOT NULL,

    server_updated_at INTEGER NOT NULL
);
```

`operation`：

```text
create
update
delete
```

`seq` 是全局递增的事件流水号，仅用于：

* 判断变化发生顺序；
* 增量同步；
* 作为客户端同步游标。

`seq` 与 `sync_version` 没有直接关系。

---

### 2.3 devices

记录参与同步的设备。

```sql
CREATE TABLE devices (
    id TEXT PRIMARY KEY,

    name TEXT,
    platform TEXT,

    last_seq INTEGER NOT NULL DEFAULT 0,

    created_at INTEGER NOT NULL,
    last_seen_at INTEGER NOT NULL,

    disabled INTEGER NOT NULL DEFAULT 0,

    last_request_seq INTEGER NOT NULL DEFAULT 0,
    last_request_hash TEXT,
    last_request_result TEXT
);
```

`last_seq` 表示该设备已确认处理到的变化流水位置，只能单调推进，不能提前确认尚未应用的响应。

`last_request_seq` 是设备最后一次完成的写请求编号，与变化游标 seq 无关。hash 与 result 仅保存该次请求及其整批结果，用于第 15 节的幂等重试。不增加 sync_meta 表。

设备 ID 不得由多个安装实例共用。丢失本地请求状态后使用新的设备 ID；禁用或重新启用不能清空该设备的幂等状态。

---

## 3. sync_version

每条 `data` 独立维护自己的 `sync_version`。

新数据从：

```text
sync_version = 1
```

开始。

每次成功执行写操作：

```text
sync_version += 1
```

包括：

* update
* delete
* forced create
* forced update
* forced delete

客户端不能指定新的 `sync_version`。

### 乐观并发控制

客户端修改已有数据时提交：

```text
base_sync_version
```

普通操作要求：

```text
base_sync_version == server.sync_version
```

否则返回：

```text
VERSION_CONFLICT
```

客户端可以向用户展示冲突，并由用户决定是否强制上传。

---

## 4. 写操作

### 4.1 Create

普通 create 只能针对逻辑上不存在的数据：

```text
记录不存在
或
deleted = true
```

成功后：

```text
deleted = false
sync_version += 1
```

全新记录：

```text
sync_version = 1
```

如果存在未删除的数据，则返回冲突。

---

### 4.2 Update

普通 update 要求：

```text
记录存在
AND
deleted = false
AND
base_sync_version == sync_version
```

成功后：

```text
content = 新内容
sync_version += 1
deleted = false
```

---

### 4.3 Delete

普通 delete 要求：

```text
记录存在
AND
deleted = false
AND
base_sync_version == sync_version
```

Delete 不接受 `content`；携带该字段返回 `INVALID_REQUEST`。

成功后：

```text
deleted = true
content = SQL NULL
sync_version += 1
```

记录不会从 `data` 中物理删除。

---

## 5. Forced

`forced` 是版本冲突后的统一逃生机制，应由用户主动选择使用。

### forced create / update

可以直接创建或覆盖当前数据。

如果记录已经存在：

```text
server.sync_version += 1
```

不得使用客户端版本覆盖或重置 `sync_version`。

### forced delete

仍要求目标数据：

```text
存在
AND
deleted = false
```

但不要求：

```text
base_sync_version == sync_version
```

成功后正常执行：

```text
deleted = true
content = SQL NULL
sync_version += 1
```

---

## 6. Change 事件

每次成功修改 `data`，必须在同一个数据库事务中创建一条 `changes`：

```text
data 修改
+
changes INSERT
```

例如：

```text
seq    device    table       id    operation
1001   iphone    archives    A     update
1002   mac       history     B     create
1003   ipad      archives    A     update
1004   mac       history     C     delete
```

`changes` 是变化事件流，不是数据历史或审计日志。

它不保存旧版 `content`。

operation 按实际状态变化记录：首次创建或恢复为 create，覆盖存活记录为 update，删除为 delete，forced 不改变该分类。每次成功写入更新 updated_by_device_id 和服务端时间；created_by_device_id 只在首次创建时设置，恢复或覆盖保持原值。

---

## 7. 增量同步

`POST /v1/sync` 请求：

```json
{
  "device_id": "...",
  "seq": 1000,
  "limit": 100
}
```

使用索引读取当前页：

```sql
SELECT seq, tablename, id, operation, device_id, server_updated_at
FROM changes
WHERE seq > ?
ORDER BY seq ASC
LIMIT ?;
```

`limit` 限制原始事件数量，不限制合并后的记录数量。流水号允许存在间隙，`next_seq` 为本页实际读取的最后一个 seq，不能计算为 client_seq + limit。没有事件时保持请求 seq。

例如读取 1001～1100 的 100 条事件，next_seq 为 1100。合并或过滤后即使结果为空，仍然推进到 1100。

## 8. 当前页合并与设备过滤

严格按以下步骤执行：

1. 按 seq 索引读取最多 limit 个事件。
2. 仅在这页内按 `(tablename, id)` 合并，保留 seq 最大的事件。
3. 按合并后的主键精确读取 data 当前状态，每个主键最多一条。
4. 根据当前 data 的 `updated_by_device_id` 排除最新版本属于请求设备的记录。
5. 返回其余当前状态。

过滤依据为当前记录的 updated_by_device_id，不单凭窗口中 change.device_id 提前排除主键，也不得查询窗口以外的 changes 来寻找最新版本。这样即使窗口截至 100，而 data 已更新到事件 105 对应的版本，也只需本页合并和主键点查。

返回的是当前状态，可能新于本页事件，不是历史快照。客户端只用较大的 sync_version 更新云端镜像；相同版本可以忽略，较旧版本不能覆盖较新状态。本地待上传修改单独保存，不能因下载被覆盖。

同一设备的写入、拉取及本地应用串行执行。成功写入结果必须先在本地落盘，保证过滤自身版本时客户端已有对应状态。

完整下载后的恢复补拉必须关闭自身过滤，见第 12 节。

## 9. 同步响应

```json
{
  "changes": [
    {
      "tablename": "archives",
      "id": "A",
      "content": {},
      "sync_version": 12,
      "deleted": false
    },
    {
      "tablename": "history",
      "id": "B",
      "sync_version": 7,
      "deleted": true
    }
  ],
  "next_seq": 1100,
  "has_more": true
}
```

next_seq 只代表扫描进度，不是返回记录的版本或对应最新事件位置。has_more 使用索引查询 next_seq 之后是否还有事件，不全表计数。

客户端在本地事务中应用响应并保存 next_seq；后续普通同步请求携带这个已提交 seq，服务端据此推进 devices.last_seq。响应尚未提交时不能推进确认位置。

## 10. 分页与查询一致性

has_more 为 true 时，用上一页 next_seq 继续请求，直到 false。新写入可能在本轮结束后出现，下一轮再获取；不要求跨记录的固定时点快照。

游标边界校验与事件页读取必须避免 GC 插入造成漏页：在同一数据库事务性读取中完成，或用等价方案验证保留边界；不能先校验，再无保护地读取已经被清理的窗口。

data 点查不得读取比已经观察到的事件更旧的数据库状态。实现时使用主库读取或可靠的顺序一致性机制，不能无约束地跨副本读取。

## 11. Changes 保留与清理

目标保留近期事件，参考配置为 MAX_CHANGES = 50000、GC_THRESHOLD = 55000。达到阈值后逐小页清理旧事件，每次至多 100 条，不执行一次性大批删除，不在每次写入时做全量检查。

禁止为判断清理时机执行 COUNT(*) 全表计数、OFFSET 跳过大量事件或无界聚合。必须保留最新一条 change；只删除最旧前缀，不能删除中间事件，也不能清空 changes。因此非空数据库可直接通过索引末端获得最新 seq，无需 sync_meta。

本实现的成功事件在事务内显式分配为最新 seq + 1，清理只删除前缀，因而自身生成的保留事件连续。截止位置采用 floor(latest_seq / 5000) * 5000 - 50000，首次在 55000 触发，之后按检查点继续。每分钟最多清理十个小页，可能暂时积压；不支持外部改写事件序列。客户端仍须容忍间隙，不能用 seq + limit 翻页。实现与验证见 [实现说明](docs/IMPLEMENTATION.md)。changes 不保存永久历史。

## 12. 完整下载与过期游标

使用 `POST /v1/full-download`，不再使用 /v1/full-sync。

### 12.1 游标边界

从 changes 的 seq 索引两端各读取最多一行，取得 oldest_seq 和 latest_seq。若 changes 为空，按协议不允许清空日志的约束，应为尚无成功写入的新库，此时 latest_seq = 0。

- seq 必须是非负安全整数；非法值或 seq > latest_seq 返回 INVALID_CURSOR。
- 非空 changes 中，`client_seq < oldest_seq - 1` 返回 FULL_SYNC_REQUIRED。
- `client_seq == oldest_seq - 1` 可以继续增量同步。
- 空库请求 seq = 0 返回空 changes、next_seq = 0、has_more = false。

### 12.2 下载流程

1. 开始下载时，通过 seq 索引末端取得最新 seq，作为 start_seq 返回客户端；该读取必须在首个 data 页读取之前完成。
2. 客户端保存 start_seq，以服务端返回的复合主键游标分页下载 data，含 tombstone；limit 默认和最大均为 100。
3. 后续页按 `(tablename, id) > (?, ?)` 定位主键索引，ORDER BY tablename, id，LIMIT ?。第一页从索引起点取小页，不使用伪造的最小字符串。
4. 下载写入客户端暂存区。完整下载不得按 updated_by_device_id 过滤任何记录。
5. 下载结束，从 start_seq 调用 /v1/sync，携带 `include_self: true`，分页补拉下载期间的变化，直到 has_more = false。此模式不提前推进 devices.last_seq。
6. 在本地事务中切换暂存数据、保留待上传操作并保存补拉后的 next_seq，然后恢复普通同步和上传。

分页游标必须无损表达两个独立字符串，不能通过特殊分隔符拼接。结束判断只允许额外索引定位读取最多一条键，不允许读取全表判断是否完成。

下载和补拉期间该设备暂停上传，其他设备可以继续写入。恢复补拉包含自身变化，避免暂存区缺少分页扫描期间由该设备先前在途写请求产生的状态。客户端仍按 sync_version 防止版本倒退。

如果 start_seq 或后续补拉游标已超出保留窗口，返回 FULL_SYNC_REQUIRED，客户端重新下载；服务端不为下载会话无限保留日志。此方案保证最终收敛，不承诺所有记录属于同一个时间点的快照。

---

## 13. Deleted Tombstone

删除的数据不得立即从 `data` 物理删除：

```text
deleted = true
```

表示该 `(tablename, id)` 当前处于删除状态。

例如：

```text
A v7 deleted=false
↓ delete
A v8 deleted=true
↓ recreate
A v9 deleted=false
```

因此 `sync_version` 在整个 `(tablename, id)` 生命周期内保持单调递增，不因删除或重新创建而重置。

---

## 14. 核心概念

```text
data
    当前完整数据状态
    服务端权威数据源

sync_version
    单条数据版本
    用于乐观并发控制

changes
    有限长度的近期变化事件流

seq
    全局变化流水号
    用于增量同步和分页

devices
    设备信息及同步状态

deleted
    删除 tombstone
    保证 Full Sync 能恢复正确状态
```

`sync_version` 回答：

> 客户端修改的是否仍然是自己曾经读取到的那个版本？

`seq` 回答：

> 客户端上次同步以后，服务器发生了哪些变化？

两者必须保持独立。


## 15. 批量写入与重试幂等

`POST /v1/write` 每次提交 1～10 个 operation：

```json
{
  "device_id": "...",
  "request_seq": 1,
  "operations": [
    {
      "operation": "create",
      "tablename": "archives",
      "id": "A",
      "content": {},
      "forced": false
    }
  ]
}
```

普通 update/delete 必须携带 base_sync_version；create 不要求该字段，forced 跳过版本匹配。forced 必须由用户主动选择。

各条操作独立判定业务成功或失败，结果按请求顺序在同一个响应的 results 数组返回，以 index 对应原操作。首版同批不允许重复 `(tablename, id)`，避免同批版本依赖；重复或非法请求结构在执行前拒绝整批。

```json
{
  "request_seq": 1,
  "results": [
    {
      "index": 0,
      "success": true,
      "tablename": "archives",
      "id": "A",
      "sync_version": 1,
      "deleted": false
    }
  ]
}
```

失败项包含 success=false、code 及可用的当前 sync_version/deleted，不包含完整 content。客户端需要内容解决冲突时通过 `/v1/read` 按主键批量读取当前状态，也可另行增量同步。成功项返回服务端版本和状态，content 由已持久化的原写请求确定；不在幂等结果中重复保存大型 content。结果回放返回的是原次提交结果，不是当前 data 状态。

整批成功项的 data 修改、对应 changes 以及 devices 中的整批请求结果在同一事务提交。业务冲突只使该项失败，不阻止其他合法项；数据库执行故障使整次事务回滚，不能存在已提交数据却无幂等结果的情况。

每设备只保存最近一次完成的写请求：

| request_seq | 行为 |
| --- | --- |
| 等于 last_request_seq 且请求 hash 相同 | 返回保存的整批结果，不重复执行 |
| 等于 last_request_seq 但 hash 不同 | REQUEST_SEQ_REUSED |
| 等于 last_request_seq + 1 | 执行请求并保存整批结果 |
| 小于 last_request_seq | REQUEST_EXPIRED |
| 大于 last_request_seq + 1 | REQUEST_OUT_OF_ORDER |

客户端先持久化完整有序请求和编号，再发送；未确认上一请求结果前不得发送下一请求。超时重发同一编号和相同请求正文；强制覆盖或修正内容使用新编号。hash 覆盖完整请求正文的 UTF-8 字节，不含主密钥；重试必须保持正文完全一致。

通过结构校验并得到确定业务结果的请求（包括全部业务失败）消耗编号并保存结果；鉴权失败、结构错误和事务故障不推进编号。服务端并发收到相同请求时也必须原子检查编号，确保最多执行一次。

## 16. 接口与错误约定

| 接口 | 用途 |
| --- | --- |
| PUT /v1/devices/{id} | 注册设备或更新名称、平台；不能重置幂等编号 |
| GET /v1/devices | 按设备主键分页列出设备，limit 默认/最大 100 |
| PATCH /v1/devices/{id} | 禁用或重新启用设备 |
| POST /v1/write | 提交最多 10 条操作，一次返回整批逐项结果 |
| POST /v1/sync | 增量下载与确认已应用游标；include_self 用于恢复补拉 |
| POST /v1/full-download | 按主键索引分页完整下载，首次返回 start_seq |
| POST /v1/read | 1～100 个二元主键点查，区分存活、已删除与不存在，返回修改信息 |
| POST /v1/table-download | 按 tablename 前缀和 id 主键范围分页，包含 tombstone，首次返回 start_seq |

接口级错误使用 HTTP 状态及 code：400 INVALID_REQUEST / INVALID_CURSOR，401 UNAUTHORIZED，403 DEVICE_DISABLED，410 FULL_SYNC_REQUIRED；请求编号错误使用 409。结构合法且处理完成的批量写入返回 HTTP 200，由 results 各项标识业务成败：ALREADY_EXISTS、ENTITY_NOT_FOUND、VERSION_CONFLICT 等。

D1 明确报告额度用尽时，读/写每日额度分别使用 429 D1_READ_QUOTA_EXCEEDED / D1_WRITE_QUOTA_EXCEEDED，账户存储与单库容量分别使用 507 D1_STORAGE_QUOTA_EXCEEDED / D1_DATABASE_SIZE_EXCEEDED。客户端停止短间隔自动重试，保留原游标和未确认请求。未知数据库故障仍使用 DATABASE_UNAVAILABLE。

流式下载已经发送 HTTP 200 后遇到额度错误，响应以顶层 error 结束，并省略成功游标和 has_more。客户端必须先检查 error，拒绝整页，不能应用此前收到的记录。未知流式故障仍中断连接。

主密钥要求见 PROJECT_SPEC.md。平台硬限制导致的失败不得截断 content 或伪报成功；事务未提交时不推进 request_seq。

## 17. 实现验收

- 所有运行时 SQL 检查执行计划，并在不同数据规模下验证读取行数仅随当前页或主键点查数量增长，不随全表规模线性增长。
- 不出现全表计数、OFFSET、大范围扫描、跨页 changes 合并或全表 data 读取；清理同样受限。
- 覆盖 limit=100、limit>100 拒绝、空字符串及特殊字符主键、JSON null、删除清空 content、恢复保留创建者。
- 覆盖 oldest_seq - 1 临界点、空库、全部事件被过滤后游标仍推进、最新版本来自自身、全量下载并发插入及恢复补拉包含自身。
- 覆盖 10 条批量混合成败、11 条拒绝、同批重复主键、相同请求并发重放、强制写入超时重试和事务回滚。

平台参考：[D1 读取计费](https://developers.cloudflare.com/d1/platform/pricing/)、[D1 硬限制](https://developers.cloudflare.com/d1/platform/limits/)。


## 18. 定向读取与单表恢复

`/v1/read` 返回顺序与 keys 一致，允许重复键。found=false 只表示 data 中无记录；已删除记录仍为 found=true、deleted=true，保留 sync_version，省略 content。存活记录 content 可以是 null。存在的记录均返回 server_updated_at 和 updated_by_device_id。每次至多 100 个键，内容每十条一组点查并流式输出，不依赖 changes 保留窗口、不确认 last_seq、不消耗 request_seq。

`/v1/table-download` 仅在指定 tablename 的主键范围内分页，每页默认/最多 100 条，包含 tombstone 和修改信息。游标结构与完整下载相同，after.tablename 必须等于请求表；首次 start_seq 在取键页之前获取，后续校验日志窗口。下载后从 start_seq 用 include_self=true 补拉全局 changes，在客户端筛选目标表并按版本应用，然后事务性切换目标表基线。期间暂停本设备上传，保留本地待上传意图；日志过期则重新下载。它不提供固定时点快照，也不能用单表恢复游标确认其他表；正式同步游标仍由普通增量同步流程管理。完整 HTTP 示例见 API 文档第 10、11 节。
