# Cloudflare D1 云同步项目规划

> 状态：新后端已实现并通过本地测试；尚未执行远端部署验收。

## 1. 目标与依据

实现单用户、自托管的 Cloudflare Workers + D1 通用 JSON 数据同步服务，配套 GitHub Pages 部署引导和本地主密钥生成页面。

[同步协议.md](同步协议.md) 是同步语义与数据库结构的依据；本文只整理实施要求，不重复维护 SQL。服务端不感知业务结构，不依赖业务表注册、字段白名单或 DOMAIN_TABLES.md。客户端新增逻辑表、字段或类型无需修改 Worker 或迁移数据库。

本次重建不承接旧后端 API、业务迁移、upsert、计数合并或全量同步会话设计，也不执行远端数据库清空或部署。

## 2. 主密钥与鉴权

系统有且仅有一个由用户管理的根凭据：`MASTER_KEY`。它是所有设备共享的 Bearer Token，适用于单用户、自托管实例。

- `MASTER_KEY` 必须使用 `crypto.getRandomValues()` 生成 32 个随机字节（256 bit 熵），并编码为 64 字符小写十六进制字符串。不得使用用户自选密码或普通随机字符串。
- 生产环境中，`MASTER_KEY` 必须配置为 Cloudflare Worker Secret，不得保存到 Wrangler 明文变量、源码、D1 或日志中。`.dev.vars` 只用于本地开发，必须被 Git 忽略；仓库只提供不含真实值的 `.dev.vars.example`。
- 客户端通过 `Authorization: Bearer <MASTER_KEY>` 调用 API。
- Worker 比较密钥时，必须先将收到的值和 Secret 分别计算为固定长度的 SHA-256 摘要，再使用 `crypto.subtle.timingSafeEqual()` 比较；不得直接使用普通字符串比较。
- 修改 Worker Secret 表示全局轮换密钥：旧密钥立即失效，所有设备都必须重新配置。Cloudflare 控制面板不能恢复或显示已保存 Secret 的原值，只能替换它。
- `device_id` 只是设备标识，不是第二重鉴权。任意设备泄露主密钥都会导致整个实例失守，解绑设备也不能撤销该设备已持有的主密钥；这是本项目为保持部署和使用简单而接受的安全边界。

## 3. 数据与读取约束

只使用同步协议中的 data、changes、devices 三张表，不增加 sync_meta。devices 保存每设备最近一次写请求的编号、hash 和整批结果。

绝对禁止全表扫描或单次全表读取，包含清理及设备查询；不能通过 LIMIT 掩盖无界扫描。changes 仅允许在当前页内比较、去重和合并；data 仅通过复合主键点查或主键索引小页下载。禁止 OFFSET、全表计数及无界排序，必须通过执行计划和读取行数验证。完整下载累计读取全部记录，每次只读有界小页。

新增 `/v1/read` 按 `(tablename,id)` 批量点查，1～100 个键，按请求顺序返回 found、当前内容、版本、删除状态、云端更新时间和修改设备，明确区分已删除与不存在。`/v1/table-download` 按 tablename 前缀及 id 主键范围分页，默认/最大 100，包含 tombstone，沿用 start_seq 与恢复补拉；只恢复目标表，不提前确认全局同步游标。两种读取均不需要新增表或索引。

## 4. 写入与增量同步

- 所有写入语义、删除恢复字段规则以同步协议为准。版本从 1 开始，删除和恢复均不重置；删除清空 content，恢复保留首次创建者。
- tablename、id 为字符串，不限制格式；content 为任意合法 JSON，不设协议大小上限。所有值使用参数绑定，仍受平台硬限制。
- /v1/write 每次最多 10 条，按操作返回同一 results 数组，允许业务结果混合成败。成功修改、changes 与整批幂等结果原子提交；数据库故障整体回滚。
- 每设备只有一条未确认写请求；原样重试返回最近一次整批结果。客户端持久化请求并按服务端版本应用结果。
- /v1/sync 按 seq 索引取当前页，在页内合并后按主键点查 data，再按 data.updated_by_device_id 过滤自身最新版本。禁止为查找最新变化扫描其他 changes 页。
- limit 默认 100、最大 100。next_seq 取实际已扫描的最后一个事件，与返回记录数量和当前版本无关；本地应用与游标保存使用同一事务。

## 5. 日志与完整下载

`client_seq < oldest_seq - 1` 才要求 FULL_SYNC_REQUIRED。日志清理只删除最旧前缀，每批最多 100 条，并保留最新一条，以便通过索引端点取得最新 seq。空 changes 对应未写入的新库，最新 seq 为 0。

/v1/full-download 首次返回 start_seq，然后按 (tablename, id) 主键索引逐页下载，包括 tombstone。客户端下载结束后从 start_seq 补拉 changes，恢复补拉包含自身事件，不提前确认 devices.last_seq。下载和补拉期间暂停本设备上传，保留本地待上传操作；追平后事务性切换并恢复普通同步。日志过期则重新下载。

该方案提供最终收敛，不提供跨记录固定时点快照。具体请求及幂等规则集中维护在同步协议中。

## 6. 实现与验证

后端按 [API 文档](docs/API.md) 实现。事务复核、流式下载、日志清理及验证范围见 [实现说明](docs/IMPLEMENTATION.md)。新库内事件在事务中连续分配，清理截止位置按每 5000 个事件的检查点推进，无需计数表或全表计数。Cron 小批清理可能暂时积压。

本地 workerd/D1 测试覆盖批量混合结果、并发重放、事务回滚、增量与完整下载、任意字符串主键、无损 JSON 数字和查询读取量。远端 D1 性能及全新账号一键部署仍待验收。

## 7. GitHub Pages

页面由标题 + 三个主体部分组成：

1. 介绍部分
  - 简要介绍 CloudFlare Worker 用途、CloudFlare D1 免费版本用量限制。
  - 详细介绍 CloudFlare Worker 部署操作步骤。
2. 生成密钥部分
  - UI 由标签 + 生成按钮 + 复制按钮 + 介绍文本组成
  - 使用 `crypto.getRandomValues()` 在浏览器本地生成 32 字节随机值，并编码为 64 字符小写十六进制 `MASTER_KEY`。
  - 明确告知用户三条规则：
    - 密钥在浏览器本地环境生成，本页面不上传不存储，并且在离开或刷新本页面后立即丢失密钥。
    - 请用户将密钥保存在密码管理器，并让客户端使用操作系统安全凭据存储；本页面和 Cloudflare 控制面板都无法恢复原值。
    - 如果密钥丢失或任意设备疑似泄露，需要在 Cloudflare Worker 控制面板中替换 Secret；替换后所有设备都要重新配置。
3. 提供 Deploy to Cloudflare 按钮。
  - 仓库配置需要声明 D1 binding，确保 Deploy 流程能自动预配 D1 并绑定 Worker。主密钥作为 Secret 必须在部署流程中由用户输入，不能放进仓库 URL。

安全要求：

- 密钥不得写入 `localStorage`、Cookie、IndexedDB、URL、分析服务或错误上报。
- 页面不加载第三方分析、广告和远程脚本。
- 离开或刷新页面后立即丢失内存中的密钥。

通过 GitHub Actions 发布 `docs/` 静态资源，无需后端依赖或构建。保留现有前端代码；新后端已提供预配配置与迁移部署脚本，一键部署仍需全新账号验收。

## 8. 安全与容量

除公开健康检查外，API 均需主密钥鉴权；生产使用 HTTPS。鉴权失败统一响应，不暴露密钥校验细节。设备禁用不等于密钥撤销。

写入批次最多 10 条，分页默认/最大 100 条；不额外限制 content 大小或 tablename、id 格式，遵守平台硬限制并控制请求频率；所有 SQL 值绑定参数，错误与日志不泄露 Secret、业务内容或内部 SQL。CORS 不能代替鉴权。

changes 有数量上限不代表总存储有上限：data 与 tombstone 会持续增长。实施前需按预期数据量核算存储及读写开销，确定容量保护，不以自动删除 tombstone 换取空间。额度以 Cloudflare 官方文档为准。

## 9. 实施与验收

1. 完成第 6 节实现设计，验证所有查询的索引访问路径与读取额度。
2. 重新建立 Worker 工程、D1 初始迁移及测试环境；先验证条件写入与 change 追加的原子性。
3. 实现主密钥鉴权、设备管理、普通及强制写入。
4. 实现增量窗口扫描、合并、设备过滤及确认游标。
5. 实现日志清理、分页全量同步和增量衔接。
6. 完成 API 文档、自动化测试及全新实例部署验收。

验收覆盖：主密钥生成与轮换；并发版本冲突；删除后恢复的版本连续性；强制操作边界；事务失败回滚；页内合并后按当前更新设备排除自身；空结果仍推进游标；多页无遗漏；清理后离线设备通过 tombstone 恢复；全量期间并发写入；最多 10 条批量混合结果与幂等重试；任意字符串主键；分页上限；非法请求与平台超限；Pages 发布不依赖后端工程。

本阶段已实现新后端，未迁移或清空远端数据。多用户登录、每设备密钥、任意 SQL、大文件和通用自动冲突合并不在项目范围内。
